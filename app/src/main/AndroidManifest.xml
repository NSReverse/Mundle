<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.nsreverse.mundle">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MundleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.AuthenticationActivity" />
        <activity android:name=".ui.SubscribedClassesActivity" />
        <activity android:name=".ui.CreateClassActivity" />
        <activity android:name=".ui.SubscribeStudentClassActivity" />
        <activity android:name=".ui.SelectedClassroomActivity" />
        <activity android:name=".ui.ClassroomFeedActivity" />
        <activity android:name=".ui.CreateFeedPostActivity" />
        <activity android:name=".ui.ClassroomAssignmentsActivity" />
        <activity android:name=".ui.CreateAssignmentActivity"></activity>
    </application>

</manifest>